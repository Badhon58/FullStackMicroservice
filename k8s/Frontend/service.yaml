apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
spec:
  selector:
    app: frontend
    role: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 31000
  type: NodePort
