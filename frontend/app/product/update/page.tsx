import Update from "@/Component/Product/Update";
import React, { Suspense } from "react";

const page = () => {
  return (
    <Suspense>
      <Update />
    </Suspense>
  );
};

export default page;
